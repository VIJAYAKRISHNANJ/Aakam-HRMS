import { ArrowLeft, Edit, Save } from "lucide-react";
import { useEffect, useState } from "react";
import type { FormEvent } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import DashboardLayout from "../components/layout/DashboardLayout";
import { PageHeader, StateMessage } from "../components/recruitment/RecruitmentComponents";
import { getTrainingErrorMessage, getTrainingProgram, updateTrainingProgram, type TrainingMode, type TrainingProgram, type TrainingStatus } from "../services/trainingService";

function EditTraining() {
  const { id } = useParams(); const navigate = useNavigate(); const [program, setProgram] = useState<TrainingProgram | null>(null); const [courseName, setCourseName] = useState(""); const [category, setCategory] = useState(""); const [trainer, setTrainer] = useState(""); const [duration, setDuration] = useState(""); const [cost, setCost] = useState(""); const [mode, setMode] = useState<TrainingMode>("ONLINE"); const [assessment, setAssessment] = useState(""); const [description, setDescription] = useState(""); const [status, setStatus] = useState<TrainingStatus>("ACTIVE"); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState("");
  useEffect(() => { if (!id) return; getTrainingProgram(id).then((data) => { setProgram(data); setCourseName(data.courseName); setCategory(data.category); setTrainer(data.trainer); setDuration(data.duration); setCost(String(data.cost)); setMode(data.mode as TrainingMode); setAssessment(data.assessment ?? ""); setDescription(data.description ?? ""); setStatus(data.status as TrainingStatus); }).catch((requestError) => setError(getTrainingErrorMessage(requestError, "Unable to load this training program."))).finally(() => setLoading(false)); }, [id]);
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!id || !courseName.trim() || !category.trim() || !trainer.trim() || !duration.trim() || !cost) return setError("Course name, category, trainer, duration, and cost are required."); const numericCost = Number(cost); if (!Number.isFinite(numericCost) || numericCost < 0) return setError("Cost must be a non-negative number."); try { setSaving(true); setError(""); await updateTrainingProgram(id, { courseName: courseName.trim(), category: category.trim(), trainer: trainer.trim(), duration: duration.trim(), cost: numericCost, mode, assessment: assessment.trim() || null, description: description.trim() || null, status }); navigate(`/training/${id}`); } catch (requestError) { setError(getTrainingErrorMessage(requestError, "Failed to update training program.")); } finally { setSaving(false); } };
  return <DashboardLayout><div className="mx-auto flex w-full max-w-4xl flex-col gap-6"><PageHeader title="Edit Training" subtitle={program ? `${program.courseName} · Update training details.` : "Update training program."} icon={Edit} /><Link to={id ? `/training/${id}` : "/training"} className="inline-flex w-fit items-center gap-2 text-sm font-semibold text-slate-600 hover:text-teal-700"><ArrowLeft size={16} />Back to training</Link>{loading ? <StateMessage type="loading">Loading training program...</StateMessage> : error && !program ? <StateMessage type="error">{error}</StateMessage> : <form onSubmit={submit} className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="font-semibold text-slate-900">Training information</h2>{error && <div className="mt-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<div className="mt-6 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Course Name<input required value={courseName} onChange={(event) => setCourseName(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3 font-normal outline-none focus:border-teal-600" /></label><label className="text-sm font-medium text-slate-700">Category<input required value={category} onChange={(event) => setCategory(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3 font-normal outline-none focus:border-teal-600" /></label><label className="text-sm font-medium text-slate-700">Trainer<input required value={trainer} onChange={(event) => setTrainer(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3 font-normal outline-none focus:border-teal-600" /></label><label className="text-sm font-medium text-slate-700">Duration<input required value={duration} onChange={(event) => setDuration(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3 font-normal outline-none focus:border-teal-600" /></label><label className="text-sm font-medium text-slate-700">Cost<input required type="number" min="0" step="0.01" value={cost} onChange={(event) => setCost(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3 font-normal outline-none focus:border-teal-600" /></label><label className="text-sm font-medium text-slate-700">Mode<select value={mode} onChange={(event) => setMode(event.target.value as TrainingMode)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 bg-white px-3 font-normal outline-none focus:border-teal-600"><option value="ONLINE">Online</option><option value="OFFLINE">Offline</option><option value="HYBRID">Hybrid</option></select></label><label className="text-sm font-medium text-slate-700">Assessment<input value={assessment} onChange={(event) => setAssessment(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3 font-normal outline-none focus:border-teal-600" /></label><label className="text-sm font-medium text-slate-700">Status<select value={status} onChange={(event) => setStatus(event.target.value as TrainingStatus)} className="mt-2 h-11 w-full rounded-lg border border-slate-300 bg-white px-3 font-normal outline-none focus:border-teal-600"><option value="ACTIVE">Active</option><option value="INACTIVE">Inactive</option></select></label><label className="text-sm font-medium text-slate-700 sm:col-span-2">Description<textarea rows={4} value={description} onChange={(event) => setDescription(event.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2 font-normal outline-none focus:border-teal-600" /></label></div><div className="mt-7 flex justify-end"><button type="submit" disabled={saving} className="inline-flex items-center gap-2 rounded-lg bg-teal-700 px-5 py-2.5 text-sm font-semibold text-white hover:bg-teal-800 disabled:opacity-60"><Save size={16} />{saving ? "Saving..." : "Save Changes"}</button></div></form>}</div></DashboardLayout>;
}
export default EditTraining;